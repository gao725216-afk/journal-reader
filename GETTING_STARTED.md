# 🚀 快速开始指南

恭喜！你的双语论文阅读器桌面版已经开发完成！🎉

## ✅ 已完成的功能

### 核心功能
- ✅ **论文库管理** - 添加、浏览、搜索论文
- ✅ **PDF阅读器** - 支持PDF和文本文件
- ✅ **思维导图** - 可视化组织论文和笔记
- ✅ **Summary编辑** - 每篇论文独立摘要
- ✅ **Markdown笔记** - 支持LaTeX数学公式
- ✅ **单词查询** - 点击查看中英文释义
- ✅ **数据持久化** - 自动保存到本地
- ✅ **多视图模式** - 库/思维导图/阅读/笔记

### UI特性
- 🎨 现代化设计（紫色渐变主题）
- 📱 响应式布局
- ✨ 流畅动画效果
- 🖱️ 优秀的用户体验

---

## 📦 安装步骤

### 1. 安装依赖

```bash
npm install
```

这将安装所有必需的依赖，包括：
- Electron 28
- React Flow (思维导图)
- React Markdown
- PDF.js
- 其他核心库

---

## 🎮 运行应用

### Web开发模式（测试现有功能）
```bash
npm run dev
```
访问：http://localhost:3000

**适用于**：测试PDF阅读、笔记、查词功能

---

### Electron开发模式（完整桌面版）⭐ **推荐**
```bash
npm run dev:electron
```

**这将启动完整的桌面应用**，包括：
- 论文库管理
- 思维导图
- 文件系统访问
- 数据持久化

---

## 🎯 使用教程

### 第一次启动

1. **运行Electron版本**
   ```bash
   npm run dev:electron
   ```

2. **界面说明**
   - 顶部：深色导航栏，显示4个视图按钮
   - 主区域：根据选择的视图显示不同内容

### 添加第一篇论文

1. **点击 "📚 论文库" 视图**
2. **点击右上角 "+ 添加论文" 按钮**
3. **选择PDF或TXT文件**
4. **论文卡片将出现在网格中**

### 阅读论文

1. **在论文库中点击任意论文卡片**
2. **自动切换到 "📖 阅读模式"**
3. **左侧：PDF查看器**
   - 缩放控制
   - 滚动浏览
   - 点击单词查询
4. **右侧上半部分：Summary编辑器**
   - 输入论文摘要
   - 自动保存
5. **右侧下半部分：详细笔记**
   - Markdown格式
   - LaTeX公式支持

### 编辑Summary

1. **在阅读模式下**
2. **在右上角的Summary编辑器中输入**
3. **会自动保存（500ms延迟）**
4. **状态显示："保存中..." → "已保存"**

### 使用思维导图

1. **点击 "🗺️ 思维导图" 视图**
2. **看到初始的中心节点**
3. **点击 "+ 添加笔记" 创建新节点**
4. **拖拽节点重新排列**
5. **连接节点（从一个节点拖到另一个）**

---

## 💡 功能展示

### 论文库视图
```
┌─────────────────────────────────────────────┐
│  📚 我的论文库              🔍 [搜索]  [+添加]│
├─────────────────────────────────────────────┤
│  ┌──────┐  ┌──────┐  ┌──────┐              │
│  │ 📕   │  │ 📕   │  │ 📝   │              │
│  │Paper1│  │Paper2│  │Paper3│              │
│  │摘要..│  │摘要..│  │摘要..│              │
│  └──────┘  └──────┘  └──────┘              │
└─────────────────────────────────────────────┘
```

### 阅读视图
```
┌────────────────────┬─────────────┐
│                    │ Summary编辑  │
│   PDF阅读器        │ ─────────── │
│                    │ 详细笔记     │
│   [缩放] [页数]    │             │
│                    │             │
└────────────────────┴─────────────┘
```

### 思维导图视图
```
┌─────────────────────────────────────┐
│ [+笔记] [💾保存]      关联论文: 3   │
├─────────────────────────────────────┤
│        ○ 中心主题                    │
│       / \                           │
│      ○   ○                          │
│    论文  笔记                        │
└─────────────────────────────────────┘
```

---

## 🔧 常见问题

### Q: 运行 `npm run dev:electron` 报错？
**A**: 确保：
1. 已运行 `npm install`
2. Node.js 版本 >= 16
3. 检查终端错误信息

### Q: Electron窗口没有打开？
**A**:
1. 检查是否有防火墙阻止
2. 查看控制台输出
3. 尝试重新运行

### Q: 添加论文按钮无响应？
**A**:
- Web模式不支持文件选择（需要Electron）
- 确保运行的是 `npm run dev:electron`

### Q: 数据保存在哪里？
**A**:
- Windows: `C:\Users\{用户名}\AppData\Roaming\journal-reader\library.json`
- 包含所有论文元数据和思维导图

### Q: PDF显示不正常？
**A**:
- 确保PDF有文本层（非扫描版）
- 大文件可能需要时间加载

---

## 🏗️ 构建Windows安装包

### 构建步骤

```bash
# 1. 构建应用
npm run build

# 2. 查看输出
ls release/0.3.0/
```

### 输出文件
```
release/
└── 0.3.0/
    ├── Journal Reader Setup 0.3.0.exe  ← Windows安装包
    └── win-unpacked/                   ← 未打包版本
```

### 安装和分发
1. **双击 `Journal Reader Setup 0.3.0.exe`**
2. **选择安装位置**
3. **完成安装**
4. **桌面将出现快捷方式**

---

## 📊 项目结构

```
journal-reader/
├── electron/              # Electron主进程
│   ├── main.ts           # 窗口、文件操作
│   └── preload.ts        # 安全API
├── src/
│   ├── components/       # UI组件
│   │   ├── LibraryView.tsx     ← 论文库
│   │   ├── PaperCard.tsx       ← 论文卡片
│   │   ├── SummaryEditor.tsx   ← 摘要编辑
│   │   ├── MindMapView.tsx     ← 思维导图
│   │   ├── DesktopApp.tsx      ← 主布局
│   │   ├── Navbar.tsx          ← 导航栏
│   │   ├── PaperViewer.tsx     ← PDF查看器
│   │   ├── MarkdownEditor.tsx  ← 笔记编辑器
│   │   └── DictionaryPopup.tsx ← 词典弹窗
│   ├── store/            # 状态管理
│   │   ├── appStore.ts         ← 阅读器状态
│   │   └── libraryStore.ts     ← 论文库状态
│   ├── types/            # TypeScript类型
│   │   └── library.ts
│   └── App.tsx           # 主应用（双模式）
├── package.json          # 依赖配置
├── vite.config.ts        # Vite配置
└── electron-vite.config.ts  # Electron配置
```

---

## 🎨 自定义配置

### 修改主题颜色
编辑 `src/components/*.css` 中的 CSS 变量：
```css
:root {
  --primary: #667eea;      /* 主色 */
  --secondary: #764ba2;    /* 次色 */
}
```

### 修改窗口大小
编辑 `electron/main.ts`:
```typescript
new BrowserWindow({
  width: 1400,  // 宽度
  height: 900,  // 高度
  ...
})
```

---

## 🚀 下一步开发建议

### 可以添加的功能
1. **真实词典API** - 集成有道、百度翻译
2. **标签管理** - 给论文打标签
3. **导出功能** - 导出思维导图为图片
4. **云同步** - 使用Firebase或其他服务
5. **深色模式** - 添加主题切换
6. **搜索优化** - 全文搜索
7. **批量导入** - 扫描整个文件夹

### 性能优化
- 虚拟滚动（大量论文时）
- PDF预加载
- 缩略图缓存

---

## 📞 获取帮助

### 文档
- `README.md` - 项目概述
- `DESKTOP_APP_GUIDE.md` - 完整实现指南
- `ELECTRON_ROADMAP.md` - 开发路线图
- `REMAINING_TASKS.md` - 任务清单

### 调试
- Electron开发工具：自动打开（开发模式）
- Console日志：`console.log('...')`
- React DevTools：已集成

---

## ✅ 测试清单

在发布前测试：

- [ ] 添加PDF论文正常
- [ ] 添加TXT论文正常
- [ ] 论文卡片显示正确
- [ ] 点击论文打开阅读器
- [ ] PDF可以缩放
- [ ] 单词查询弹窗正常
- [ ] Summary自动保存
- [ ] Markdown笔记正常
- [ ] LaTeX公式渲染正常
- [ ] 思维导图可以拖拽
- [ ] 视图切换流畅
- [ ] 数据重启后保留
- [ ] 搜索功能正常

---

## 🎉 恭喜！

你现在拥有一个功能完整的**桌面版论文管理系统**！

**立即开始：**
```bash
npm install
npm run dev:electron
```

**祝你使用愉快！📚✨**

---

**版本**: v0.3.0
**最后更新**: 2025-01-19
**技术栈**: React 18 + Electron 28 + TypeScript + React Flow
